#!/usr/bin/env bash
# fzf --preview command for directories, files, file:line jump targets, and
# executables. fzf may run it through sh, which lacks the Homebrew PATH.
export PATH="/opt/homebrew/bin:$HOME/.local/bin:$PATH"

# Strip the tab-separated description of a fish completion.
p="${1%%	*}"

if [ -d "$p" ]; then
  eza -1a --color=always --icons --group-directories-first "$p" 2>/dev/null || ls -la "$p"
elif [ -f "$p" ]; then
  bat --color=always --style=numbers --line-range :300 "$p" 2>/dev/null || cat "$p"
elif [[ "$p" =~ ^(.+):([0-9]+)$ ]] && [ -f "${BASH_REMATCH[1]}" ]; then
  file="${BASH_REMATCH[1]}"
  line="${BASH_REMATCH[2]}"
  bat --color=always --highlight-line "$line" --line-range "$line:+100" "$file" 2>/dev/null ||
    cat "$file"
elif command -v "$p" >/dev/null 2>&1; then
  whatis "$p" 2>/dev/null
  echo ""
  "$p" --help 2>&1 | head -30
else
  echo "$p"
fi
