# ── Dotfiles directory ───────────────────────────────
# Rendered by chezmoi from its own source location, so dots, sb, vm, and
# the LazyVim <leader>fC picker all agree on the repo path.
set -gx DOTFILES_DIR {{ .chezmoi.workingTree | quote }}
