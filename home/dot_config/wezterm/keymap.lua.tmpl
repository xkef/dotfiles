-- The WezTerm leader from .chezmoidata/keys.toml, the only value chezmoi
-- fills in, so every other module stays plain Lua.
return {
  key = "{{ .keys.prefix }}",
  mods = "{{ .keys.prefix_mods }}",
}
