# ── Plugins (TPM) ───────────────────────────────────
# @key tmux :: I :: Install TPM plugins
# TPM installs and loads plugins under $TMUX_PLUGIN_MANAGER_PATH.
# See https://github.com/tmux-plugins/tpm
set-environment -g TMUX_PLUGIN_MANAGER_PATH "$HOME/.local/share/tmux/plugins"

set -g @plugin 'tmux-plugins/tpm'

# @key tmux :: t :: Thumbs (hint-copy visible text)
# tmux-thumbs labels URLs, hashes, IPs, and paths with hint characters, like
# vim-easymotion, and copies the picked one.
# See https://github.com/fcsonline/tmux-thumbs
set -g @plugin 'fcsonline/tmux-thumbs'
set -g @thumbs-key t
# Copy to the system clipboard on macOS, Wayland, or X11.
set -g @thumbs-command 'echo -n {} | (pbcopy 2>/dev/null || wl-copy 2>/dev/null || xclip -sel clip 2>/dev/null)'

# @key tmux :: u :: Open URL from scrollback (fzf)
# fzf picks from the URLs in the pane's history.
# See https://github.com/wfxr/tmux-fzf-url
set -g @plugin 'wfxr/tmux-fzf-url'
set -g @fzf-url-bind u
set -g @fzf-url-fzf-options '-w 60% -h 40% --prompt="  " --border-label=" Open URL "'
set -g @fzf-url-history-limit '2000'

# ── TPM bootstrap ───────────────────────────────────
# Install TPM on first run without blocking startup, then load it.
if "test ! -d $HOME/.local/share/tmux/plugins/tpm" \
   "run-shell -b 'git clone https://github.com/tmux-plugins/tpm $HOME/.local/share/tmux/plugins/tpm && $HOME/.local/share/tmux/plugins/tpm/bin/install_plugins'"

if "test -x $HOME/.local/share/tmux/plugins/tpm/tpm" \
   "run '$HOME/.local/share/tmux/plugins/tpm/tpm'"
