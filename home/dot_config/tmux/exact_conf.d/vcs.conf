# ── VCS popups ───────────────────────────────────────
# @key tmux :: g :: Lazygit (floating popup)
bind g if-shell 'command -v lazygit >/dev/null 2>&1' \
  "display-popup -d '#{pane_current_path}' -w 90% -h 90% -E lazygit" \
  "display-message 'lazygit is not installed'"

# @key tmux :: G :: jjui (floating popup)
bind G if-shell 'command -v jjui >/dev/null 2>&1' \
  "display-popup -d '#{pane_current_path}' -w 90% -h 90% -E jjui" \
  "display-message 'jjui is not installed'"

# @key tmux :: R :: Scooter search & replace (popup)
bind R if-shell 'command -v scooter >/dev/null 2>&1' \
  "display-popup -d '#{pane_current_path}' -w 90% -h 90% -E scooter" \
  "display-message 'scooter is not installed'"

# @key tmux :: d :: Lazydocker (floating popup)
bind d if-shell 'command -v lazydocker >/dev/null 2>&1' \
  "display-popup -d '#{pane_current_path}' -w 90% -h 90% -E lazydocker" \
  "display-message 'lazydocker is not installed'"
