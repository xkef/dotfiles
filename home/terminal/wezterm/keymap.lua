-- The WezTerm leader. Ctrl-Space pairs with the Neovim leader, Space,
-- after wincent, so one physical key serves both. Ctrl-A and Ctrl-B stay
-- free for readline.
return {
  key = "Space",
  mods = "CTRL",
}
