# Tasks

## 1. API

- [x] 1.1 `tether.api` with registration, notifications, and whitelisted
      services
- [x] 1.2 Facade `turns()`, `on`, `register`; registered subcommands and
      completion
- [x] 1.3 `types.lua` and the API section of `doc/tether.txt`
- [x] 1.4 Tests: register a picker source, register a cockpit section, register
      a review header, register a command, subscribe to finished turns,
      unsubscribe, turn snapshots are copies, features stay behind the API,
      every public function is documented
