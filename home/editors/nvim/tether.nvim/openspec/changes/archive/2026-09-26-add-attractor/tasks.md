# Tasks

## 1. Parser and lint

- [x] 1.1 Tokenizer and parser with Fabro shapes, BFS order
- [x] 1.2 Lint rules and diagnostics; `fabro validate` findings
- [x] 1.3 Tests: chained edges, handler from shape, subgraph defaults, missing
      exit, unreachable node, valid example

## 2. Navigation

- [x] 2.1 `nodes` picker source and outline buffer
- [x] 2.2 Tests: outline order

## 3. Runs

- [x] 3.1 `http` module: GET, POST, SSE parsing through `curl`
- [x] 3.2 Backend interface, spec backend (directory and HTTP), Fabro backend
      (settings, stream resume, questions, answers, launch)
- [x] 3.3 Active run, virtual text, cockpit Pipeline section, answer command
- [x] 3.4 Tests: completed and failed nodes, stage started from a spec server,
      Fabro stream updates nodes, Fabro server from settings, spec answer, Fabro
      answer, Fabro yes or no

## 4. Fabro workflow

- [x] 4.1 `range` review scope; launch and run review commands
- [x] 4.2 Tests: launch attaches, review a Fabro run
- [x] 4.3 README and vimdoc
