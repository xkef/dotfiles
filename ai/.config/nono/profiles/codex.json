{
  "$schema": "https://nono.dev/schemas/nono-profile.schema.json",
  "extends": "codex",
  "meta": {
    "name": "codex",
    "version": "1.0.0",
    "description": "Codex with mise, jj, maven, npm, and cargo access"
  },
  "filesystem": {
    "allow": [
      "$HOME/.m2",
      "$HOME/.npm",
      "$XDG_CACHE_HOME/mise",
      "$XDG_CONFIG_HOME/jj",
      "$XDG_DATA_HOME/cargo"
    ],
    "allow_file": ["$HOME/Library/Keychains/login.keychain-db"],
    "read": [
      "$XDG_DATA_HOME/mise",
      "$XDG_STATE_HOME/mise",
      "$XDG_CONFIG_HOME/gh"
    ],
    "bypass_protection": ["$HOME/Library/Keychains/login.keychain-db"]
  }
}
