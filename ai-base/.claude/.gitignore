/.credentials.json
/local
/plans
/projects
/shell-snapshots
/stats-cache.json
/statsig
/todos
__store.db

# `~/.claude/skills/` is a symlink to the shared `.agents/skills/` tree.
# If it is ever replaced by a directory, ignore generated/upstream skills.
/skills/*
