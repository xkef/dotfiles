#!/bin/sh
# Launch kickstart-style nvim with isolated config/data/state/cache.
# Config lives in nvim/.config/kickstart/ (stowed alongside lazyvim).
# See: :help NVIM_APPNAME
NVIM_APPNAME=kickstart exec nvim "$@"
